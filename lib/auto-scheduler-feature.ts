/** Flip to true when Auto Scheduler is ready to ship again. */
export const AUTO_SCHEDULER_ENABLED = false;

/** Flip to true when Scheduling rules settings and grid highlights are ready to ship. */
export const SCHEDULING_RULES_ENABLED = false;

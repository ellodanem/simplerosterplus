/** Calendar month bucket for messaging caps (`YYYY-MM` in UTC). */
export function messagingMonthKey(date = new Date()): string {
  const y = date.getUTCFullYear();
  const m = String(date.getUTCMonth() + 1).padStart(2, "0");
  return `${y}-${m}`;
}
